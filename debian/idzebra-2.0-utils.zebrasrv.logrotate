/var/log/zebrasrv.log {
	weekly
	missingok
	rotate 4
	compress
	delaycompress
	notifempty
	postrotate
		if [ -d /run/systemd/system ]; then
			/usr/bin/systemctl try-reload-or-restart zebrasrv > /dev/null
		fi
	endscript
}
