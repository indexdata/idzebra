#!/bin/sh

set -e

case "$1" in
    remove|deconfigure)
        update-alternatives --quiet --remove zebraidx /usr/bin/zebraidx-1.3
        update-alternatives --quiet --remove zebrasrv /usr/bin/zebrasrv-1.3
        ;;
    *)
esac
